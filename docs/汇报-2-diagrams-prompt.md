# 图表提示词：医患对话智能病历生成系统 - 图表集合

## 1. 图表概述
- **类型**：多图表集合（流程图、类图、甘特图）
- **布局策略**：每个图表独立，水平排列
- **画布尺寸**：1200x600 px（每个图表）

---

## 图表一：研究背景痛点示意图

### 1.1 元素清单

| 元素名称 | 形状类型 | 配色方案 | 图标 | 尺寸 |
|----------|----------|----------|------|------|
| 医生 | 人物图标 | #1E3A8A(边框), #FFFFFF(背景) | 医生图标 | 80x80 px |
| 患者 | 人物图标 | #2ECC71(边框), #FFFFFF(背景) | 患者图标 | 80x80 px |
| 病历本 | 矩形 | #B03A2E(边框), #FFFFFF(背景) | 文档图标 | 60x60 px |
| 电脑/键盘 | 矩形 | #566573(边框), #FFFFFF(背景) | 电脑图标 | 60x60 px |
| 时钟 | 圆形 | #E74C3C(边框), #FFFFFF(背景) | 时钟图标 | 50x50 px |
| 注意力分散箭头 | 箭头 | #F39C12(填充) | 无 | - |

### 1.2 布局说明
- 左侧：医生正在与患者交谈，同时手写病历（注意力分散表示）
- 中间：键盘输入病历的动作
- 右侧：病历本显示复杂的医学术语
- 顶部：时钟显示时间压力
- 用红色虚线箭头表示"注意力分散"

### 1.3 文字内容
- 标题：门诊场景痛点
- 注释：边问诊、边记录、边思考 → 三重认知负担

---

## 图表二：系统数据流程图

### 2.1 元素清单

| 元素名称 | 形状类型 | 配色方案 | 图标 | 尺寸 |
|----------|----------|----------|------|------|
| 门诊录音 | 圆角矩形 | #1E3A8A(边框), #E8F4FD(背景) | 麦克风图标 | 140x60 px |
| ASR模块 | 圆角矩形 | #27AE60(边框), #E8F8F0(背景) | ASR图标 | 160x80 px |
| 术语纠错 | 圆角矩形 | #F39C12(边框), #FEF9E7(背景) | 术语图标 | 160x80 px |
| 病历生成 | 圆角矩形 | #9B59B6(边框), #F5EEF8(背景) | 文档生成图标 | 160x80 px |
| SOAP病历 | 圆角矩形 | #1ABC9C(边框), #E8FDF8(背景) | 病历图标 | 140x60 px |
| 错误检测 | 圆角矩形 | #E74C3C(边框), #FDEDEC(背景) | 错误检测图标 | 140x60 px |

### 2.2 布局说明
- 水平流程图，从左到右
- 每个模块之间用箭头连接
- 错误检测模块用虚线箭头指回术语纠错模块（形成闭环）

### 2.3 文字内容
- 输入：门诊录音（mp3/wav）
- 中间节点：ASR转写 → 术语纠错 → 结构化生成 → 错误检测
- 输出：SOAP格式结构化病历
- 反馈：检测到错误 → 重新纠错

---

## 图表三：ASR模块类设计图

### 3.1 元素清单

| 元素名称 | 形状类型 | 配色方案 | 图标 | 尺寸 |
|----------|----------|----------|------|------|
| ASRBase | 抽象类矩形 | #2C3E50(边框), #EBEDEF(背景) | 抽象类图标 | 200x80 px |
| FunASREngine | 具体类矩形 | #27AE60(边框), #E8F8F0(背景) | 具体类图标 | 180x70 px |
| WeNetEngine | 具体类矩形 | #3498DB(边框), #EBF5FB(背景) | 具体类图标 | 180x70 px |
| WhisperEngine | 具体类矩形 | #9B59B6(边框), #F5EEF8(背景) | 具体类图标 | 180x70 px |
| ASRFactory | 工厂类矩形 | #E67E22(边框), #FEF5E7(背景) | 工厂图标 | 160x60 px |
| <<interface>> | 接口标签 | - | 无 | - |

### 3.2 布局说明
- 顶部：ASRBase抽象类
- 中间：三个具体引擎实现类（水平排列）
- 底部：ASRFactory工厂类
- 用虚线箭头表示"实现"关系
- 用实线箭头表示"创建"关系

### 3.3 文字内容
- ASRBase: + transcribe(), + get_confidence(), + set_hotwords()
- FunASREngine: - model_path, - vad_enabled
- ASRFactory: + create_engine(type), + register()

---

## 图表四：实现难点与解决方案对比图

### 4.1 元素清单

| 元素名称 | 形状类型 | 配色方案 | 图标 | 尺寸 |
|----------|----------|----------|------|------|
| 难点卡片 | 卡片组 | #E74C3C(边框), #FDEDEC(背景) | 问题图标 | 300x100 px |
| 方案卡片 | 卡片组 | #27AE60(边框), #E8F8F0(背景) | 解决方案图标 | 300x100 px |
| 对比箭头 | 双向箭头 | #2C3E50 | 无 | - |

### 4.2 布局说明
- 三行对比布局
- 每行：难点（左） → 方案（右）
- 用颜色区分问题（红色调）和解决方案（绿色调）

### 4.3 文字内容
- 行1：医疗术语识别率低 → 热词表 + 拼音纠错
- 行2：长音频内存溢出 → VAD自动切分（≤30s）
- 行3：说话人分离困难 → CAM++声纹识别 + 交替映射兜底

---

## 图表五：项目进度甘特图

### 5.1 元素清单

| 元素名称 | 形状类型 | 配色方案 | 图标 | 尺寸 |
|----------|----------|----------|------|------|
| 已完成条 | 进度条 | #27AE60(填充) | 勾选图标 | 宽度按比例 |
| 进行中条 | 进度条 | #F39C12(填充) | 圆点图标 | 宽度按比例 |
| 未开始条 | 进度条 | #BDC3C7(填充) | 无 | 宽度按比例 |
| 当前时间线 | 垂直线 | #E74C3C(2px虚线) | 无 | - |

### 5.2 布局说明
- 左侧：任务名称列表
- 中间：时间轴（月份）
- 右侧：进度条显示
- 垂直虚线标记当前时间点（2026年4月）

### 5.3 文字内容
- 任务行：
  1. 调研与架构设计 [1月-2月] ████████████ 已完成
  2. ASR模块实现 [2月-3月] ████████████ 已完成
  3. Web应用框架 [2月-3月] ████████████ 已完成
  4. 术语纠错模块 [4月-5月] ████░░░░░░░░░░░ 进行中
  5. SOAP病历生成 [5月-6月] ░░░░░░░░░░░░░░░░ 未开始
  6. 系统集成测试 [6月-7月] ░░░░░░░░░░░░░░░░ 未开始

---

## 图标来源
统一使用 Heroicons 图标库：
- 医生图标：user-circle（solid）
- 患者图标：user（outline）
- 麦克风图标：microphone
- 文档图标：document-text
- 电脑图标：computer-desktop
- 时钟图标：clock
- 术语图标：book-open
- 错误检测图标：exclamation-triangle
- 勾选图标：check-circle
